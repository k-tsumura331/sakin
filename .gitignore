# Node
node_modules/
dist/
*.tsbuildinfo
npm-debug.log*

# Real data (should live in SAKIN_HOME, never in the repo — this is a
# second line of defense against accidental placement inside the repo tree)
*.db
*.db-journal
*.db-wal
*.db-shm
*.jsonl
data/
themes/*.yaml
!themes/example.yaml

# Env / secrets
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.vscode/
.idea/
